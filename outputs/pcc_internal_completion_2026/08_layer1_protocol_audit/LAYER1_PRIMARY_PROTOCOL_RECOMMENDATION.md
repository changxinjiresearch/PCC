# Layer 1 primary-protocol recommendation

**Recommendation:** retain Formal v1 (10+10 epochs) as the provisional primary protocol and Formal v1.1 (15+15 epochs) as a prespecified training-duration sensitivity analysis.

The recommendation is based on scientific provenance and naming: archived notebook cell 75 defines Formal v1, whereas cell 78 explicitly defines v1.1 as a 15-epoch sensitivity test. It is not based on which Dice is higher. Both have comparable patient isolation, leakage controls, reproducibility, and methodological clarity.

Final status: `HUMAN_DECISION_REQUIRED`.

Before manuscript lock, the authors should confirm that (1) Formal v1 was prospectively intended as the primary Layer 1 protocol, (2) train-calibrated threshold is the intended primary endpoint rather than fixed 0.5, and (3) Layer 1 is described as current-lesion segmentation with a trained residual corrector, not as the Layer 2 retrospective PCC mechanism.
