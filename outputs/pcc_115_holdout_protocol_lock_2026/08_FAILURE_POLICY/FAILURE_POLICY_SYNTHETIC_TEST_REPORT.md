# Failure-policy synthetic test report

The locked YAML is tested for a 115-patient denominator, complete-case reporting, conservative paired-difference zero assignment for one-method and both-method failures, non-finite failure classification, immutable first-attempt logs and a maximum of three attempts. No patient result was used.

Result: PASS. Verified by saved pytest output; relevant checks protocol test 32 passes.
